# Build / packaging artifacts
*.plasmoid
*.zip
*.tar.gz
build/
dist/

# Generated i18n artifacts. The .po files are the committed source (human-authored translations);
# the .pot template and the compiled .mo catalogs are BOTH regenerated by `make messages` / `make i18n`
# from the QML + .po, so they are derivable noise (the .pot diff is pure date + line-number churn).
po/*.pot
package/contents/locale/

# KDE / Qt scratch files
.directory
*.kdev4
.kdev4/
*.qmlc
*.jsc

# Editor / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
